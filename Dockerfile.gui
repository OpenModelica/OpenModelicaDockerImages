FROM fedora:30

MAINTAINER Martin Sjölund <martin.sjolund@liu.se>

RUN dnf install -y 'dnf-command(config-manager)' && \
    dnf config-manager --add-repo https://build.openmodelica.org/rpm/fc30/omc.repo && \
    dnf install --setopt=install_weak_deps=False --best -y openmodelica-1.9 && \
    dnf clean all && \
    rm -rf /var/cache/yum*
