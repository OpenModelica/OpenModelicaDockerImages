FROM openmodelica/openmodelica:v1.13.2-minimal

MAINTAINER Martin Sjölund <martin.sjolund@liu.se>

RUN apt-get update && apt-get install -qy openmodelica libomccpp \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
