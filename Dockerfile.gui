FROM openmodelica/openmodelica:v1.14.2-ompython

MAINTAINER Martin Sjölund <martin.sjolund@liu.se>

RUN apt-get update && apt-get install -qy openmodelica \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
