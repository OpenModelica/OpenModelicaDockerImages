ARG BASE

FROM ${BASE}

LABEL MAINTAINER Martin Sjölund <martin.sjolund@liu.se>

RUN export DEBIAN_FRONTEND="noninteractive" && apt-get update && apt-get install -qy openmodelica \
    && (uname -m | grep -E "arm|aarch64" || apt-get install -qy libomccpp) \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
