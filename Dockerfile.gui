ARG BASE

FROM ${BASE}

MAINTAINER Martin Sjölund <martin.sjolund@liu.se>

RUN export DEBIAN_FRONTEND="noninteractive" && apt-get update && apt-get install -qy openmodelica libomccpp \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
